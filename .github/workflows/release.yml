name: Release (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read
  checks: read

jobs:
  deprecated:
    name: Deprecated workflow notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: |
          echo "This workflow is deprecated."
          echo "Use package_core.yml, which now performs version bump, tagging, and release publish at the end of a successful run."
